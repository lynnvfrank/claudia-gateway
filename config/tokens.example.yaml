# Copy to `tokens.yaml` and replace tokens. Reload on mtime.
# Bearer value must match what clients send (Continue apiKey, curl, etc.).

tokens:
  - label: personal
    token: "claudia-personal-replace-me"
    tenant_id: "personal"
  - label: partner
    token: "claudia-partner-replace-me"
    tenant_id: "partner"
