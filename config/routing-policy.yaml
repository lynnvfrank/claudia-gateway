# Generated routing policy — virtual Claudia model only.
# Regenerated by the operator UI; see config/routing-policy.yaml in the repo for field semantics.

ambiguous_default_model: groq/llama-3.3-70b-versatile
rules:
    - models:
        - groq/llama-3.3-70b-versatile
      name: long-user-turn
      when:
        min_message_chars: 8000
    - models:
        - groq/llama-3.3-70b-versatile
      name: default
      when: {}
